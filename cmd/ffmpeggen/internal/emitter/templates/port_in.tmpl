{{- /* port_in.tmpl — generates internal/ports/in/{domain}_gen.go */ -}}
// Code generated by ffmpeggen. DO NOT EDIT.

package in

import "unsafe"

var _ = unsafe.Pointer(nil) // ensure import

{{- if .PublicType}}

// {{.PublicType}} is the public interface for {{.Name}} domain operations.
// It exposes struct field accessors and lifecycle methods.
// Low-level C API methods are available on the concrete wrapper type
// but are not part of this interface contract.
type {{.PublicType}} interface {
{{- range .Accessors}}
	{{.GoName}}() {{.GoType}}
{{- if .HasSetter}}
	Set{{.GoName}}(v {{.GoType}})
{{- end}}
{{- end}}
	Free()
	Ptr() unsafe.Pointer
}
{{- end}}
