// Code generated by ffmpeggen. DO NOT EDIT.

package ffmpeg

import (
	"sync"

	"github.com/bnema/purego-ffmpeg/internal/capi"
	"github.com/bnema/purego-ffmpeg/internal/core"
	"github.com/bnema/purego-ffmpeg/internal/loader"
	portout "github.com/bnema/purego-ffmpeg/internal/ports/out"
)

// Option configures Init.
type Option = loader.Option

// SOVersions specifies SO major version numbers.
type SOVersions = loader.SOVersions

// WithDir sets the directory to search for FFmpeg shared libraries.
func WithDir(dir string) Option { return loader.WithDir(dir) }

// WithSOVersions overrides the default SO version numbers.
func WithSOVersions(v SOVersions) Option { return loader.WithSOVersions(v) }

var (
	initOnce sync.Once
	initErr  error
	bridge   *capi.Bridge
)

// Init loads FFmpeg libraries and wires all adapters.
// Init is safe for concurrent use — it runs only once via sync.Once.
// Subsequent calls return the cached result, even with different options.
// Callers must ensure Init() has returned before calling any FFmpeg
// functions from other goroutines.
func Init(opts ...Option) error {
	initOnce.Do(func() {
		handles, err := loader.Load(opts...)
		if err != nil {
			initErr = err
			return
		}
		capi.Register(capi.Handles(handles))
		bridge = capi.NewBridge()
		core.Strerror = bridge.Util.Strerror
	})
	return initErr
}

// Shutdown performs global cleanup. Currently a no-op — FFmpeg library
// handles are released when the process exits. Reserved for future use.
func Shutdown() {}

func defaultFormat() portout.FormatCAPI {
	if bridge == nil {
		panic("ffmpeg: Init() must be called before using FFmpeg functions")
	}
	return bridge.Format
}

func defaultCodec() portout.CodecCAPI {
	if bridge == nil {
		panic("ffmpeg: Init() must be called before using FFmpeg functions")
	}
	return bridge.Codec
}

func defaultPacket() portout.PacketCAPI {
	if bridge == nil {
		panic("ffmpeg: Init() must be called before using FFmpeg functions")
	}
	return bridge.Packet
}

func defaultFrame() portout.FrameCAPI {
	if bridge == nil {
		panic("ffmpeg: Init() must be called before using FFmpeg functions")
	}
	return bridge.Frame
}

func defaultSwscale() portout.SwscaleCAPI {
	if bridge == nil {
		panic("ffmpeg: Init() must be called before using FFmpeg functions")
	}
	return bridge.Swscale
}

func defaultSwresample() portout.SwresampleCAPI {
	if bridge == nil {
		panic("ffmpeg: Init() must be called before using FFmpeg functions")
	}
	return bridge.Swresample
}

func defaultDict() portout.DictCAPI {
	if bridge == nil {
		panic("ffmpeg: Init() must be called before using FFmpeg functions")
	}
	return bridge.Dict
}

func defaultUtil() portout.UtilCAPI {
	if bridge == nil {
		panic("ffmpeg: Init() must be called before using FFmpeg functions")
	}
	return bridge.Util
}

func defaultHWAccel() portout.HWAccelCAPI {
	if bridge == nil {
		panic("ffmpeg: Init() must be called before using FFmpeg functions")
	}
	return bridge.HWAccel
}

func defaultStream() portout.StreamCAPI {
	if bridge == nil {
		panic("ffmpeg: Init() must be called before using FFmpeg functions")
	}
	return bridge.Stream
}

func defaultFilter() portout.FilterCAPI {
	if bridge == nil {
		panic("ffmpeg: Init() must be called before using FFmpeg functions")
	}
	return bridge.Filter
}
