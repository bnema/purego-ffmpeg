package mocks

import in "github.com/bnema/purego-ffmpeg/internal/ports/in"

// AVRational aliases in.AVRational so that mock methods satisfy
// the public interfaces when passing this value type.
type AVRational = in.AVRational
